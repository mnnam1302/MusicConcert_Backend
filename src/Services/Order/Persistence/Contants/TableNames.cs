namespace Persistence.Contants;

internal class TableNames
{
    internal const string TicketInfo = nameof(TicketInfo);
    internal const string CustomerInfo = nameof(CustomerInfo);

    internal const string Orders = nameof(Orders);
    internal const string OrderDetails = nameof(OrderDetails);

    internal const string OutboxMessages = nameof(OutboxMessages);
}